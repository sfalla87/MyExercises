# include <stdio.h>
#include <stdlib.h>
int main(void)
{
	int num,prim,i,primo;
	printf("Introduzca un numero\n");
	scanf("%d",&num);
	for(i=num;prim=1;i++) //en cuanto ponga 1 a la variable prim se parara el bucle
	{
		for(;;)//ahora trataremos de encontrar el num primo
		{
			if(i%x==0)
			{
				if(x!=1)
				{
				 //el num no es primo
				}
			}	
		}
	}
	printf("EL Nº PRIMO + PROXIMO MAYOR A Nº ES %d ",primo);
	system("PAUSE");
}
